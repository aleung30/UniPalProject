package ui;

import javax.swing.event.DocumentEvent;
import javax.swing.event.DocumentListener;

public class ViewMoodButtonSearchListener implements DocumentListener {

    /*
     * EFFECTS: called when text is inserted into the field
     */
	@Override
	public void insertUpdate(DocumentEvent e) {
	
	}

    /*
     * EFFECTS: called when text is removed from the field
     */
	@Override
	public void removeUpdate(DocumentEvent e) {
	
	}

    /*
     * EFFECTS: called when text is removed from the field
     */
	@Override
	public void changedUpdate(DocumentEvent e) {

	}


}
